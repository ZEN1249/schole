import java.util.Arrays;

public class Sorter {
    public static double[] posortuj(double a, double b, double c) {
        double[] arr = {a, b, c};
        Arrays.sort(arr);
        return arr;
    }
}
